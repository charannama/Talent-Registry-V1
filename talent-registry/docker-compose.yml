version: '3.8'

services:
  mailhog:
    image: mailhog/mailhog:latest
    container_name: talent_registry_mailhog
    ports:
      - "1025:1025" # SMTP
      - "8025:8025" # Web UI
    restart: always
