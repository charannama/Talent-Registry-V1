package com.zencube.registry.dashboard.mapper;

import org.springframework.stereotype.Component;

@Component
public class DashboardMapper {
    // Currently, mapping logic will be handled directly in the Service due to the aggregated nature of the queries,
    // but this component can be expanded if entity-to-DTO conversion for specific analytic drill-downs is needed.
}
